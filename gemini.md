태양광 인버터 Modbus 매뉴얼(PDF)을 파싱하여 레지스터 맵을 추출하고 변환하는 '모델메이커(Model Maker)' 웹 어플리케이션을 개발하려고 해. 아래 요구사항에 맞춰서 백엔드(Python)와 프론트엔드(HTML/JS) 코드를 전부 작성해 줘.

[참조 데이터 (매우 중요)]
- 현재 프로젝트 디렉토리 내에 `example/input/` 폴더와 `example/output/` 폴더가 있어.
- `example/input/` 폴더에는 변환할 원본 대상인 인버터 매뉴얼 PDF 샘플들이 들어 있어.
- `example/output/` 폴더에는 최종적으로 변환되어야 할 정답지 양식이 들어 있어.
- 코드를 작성할 때 반드시 이 출력 폴더의 파일 구조(Category, Address, Register Name 등 컬럼명과 데이터 형식)를 분석하고 레퍼런스로 삼아. 특히 AI 모델에게 전달할 프롬프트를 구성할 때, 이 출력 파일의 양식을 Few-shot 예시로 사용하도록 하드코딩하거나 동적으로 읽어오게 만들어 줘.

[기술 스택]
- 백엔드: 파이썬 FastAPI
- 프론트엔드: 순수 HTML, CSS, JavaScript (비동기 Fetch API 사용)
- AI 모델: Hugging Face의 `google/gemma-4-E4B` (Vision 기능 활용)
- PDF 처리: PyMuPDF (fitz)
- 데이터 처리: Pandas

[UI/UX 화면 요구사항]
1. 사용자가 직접 PDF 파일을 첨부할 수 있는 파일 업로드 영역이 있어야 해.
2. 화면에 변환 작업의 전체 흐름(1단계 -> 2단계 -> 3단계)이 시각적으로 명확히 보여야 해.
   - 1단계: PDF -> 레지스터 맵 파싱 (엑셀 변환)
   - 2단계: UDP-서버 레지스터 맵 매칭
   - 3단계: Register.py 생성
3. 사용자가 파일을 올리고 '1단계 실행' 버튼을 직접 누르면 백엔드로 요청이 가도록 만들어 줘.
4. 1단계 실행이 완료되면, AI가 파싱한 결과(JSON/Table)가 화면에 출력되어 사람이 눈으로 검증할 수 있어야 해.

[백엔드 핵심 로직 (1단계 중심)
1. 업로드된 PDF의 특정 페이지를 PyMuPDF로 고해상도 이미지로 변환해.
2. transformers 라이브러리를 사용해 `google/gemma-4-E4B` 모델을 로드해 (VRAM 최적화를 위해 torch.float16 사용).
3. PDF 이미지와 프롬프트(example/output 양식 참조)를 모델에 전달해서 표 데이터를 JSON 배열로 추출해.
4. 추출된 JSON을 Pandas를 이용해 목표 엑셀 양식에 맞춘 데이터프레임으로 변환 후 프론트엔드로 응답해 줘.

위 요구사항을 충족하는 (1) app.py (백엔드 코드)와 (2) index.html (프론트엔드 코드)의 풀 버전을 작성해 줘.